name: Link Integrity

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    uses: SixStudioGroup/velocity-architecture/.github/workflows/link-integrity.yml@main
    with:
      site_url: 'https://sixstudiogroup.github.io/trading-dashboard/'
      repo_name: 'trading-dashboard'
