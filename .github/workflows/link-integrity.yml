name: Link Integrity

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    # Shared reusable workflow still lives in the ZenCloudAU org; update this if
    # velocity-architecture is also moved to SixStudioGroup.
    # Pinned to a commit SHA (was @main) for supply-chain integrity; bump the
    # SHA deliberately when the reusable workflow is updated upstream.
    uses: ZenCloudAU/velocity-architecture/.github/workflows/link-integrity.yml@db025022a5ebe9b5d04c30424d1cad0207d60203 # main
    with:
      # Hosting moved off GitHub Pages (which 404s for a private repo) to
      # Cloudflare Pages. Update this to the custom domain once one is attached.
      site_url: 'https://sixquant.pages.dev/'
      repo_name: 'trading-dashboard'
